# 코딩테스트 문제 풀이 학습 가이드

## 목표
- Python으로 문제를 읽고 적절한 자료구조와 알고리즘을 스스로 선택하는 감각을 만든다.
- 기존 풀이가 있어도 정답 코드를 먼저 보지 않고 새 문제처럼 다시 푼다.
- 문제 수보다 제한 시간 안의 접근, 시간복잡도 판단, 오답 원인, 재풀이를 중요하게 본다.

## 문제를 풀기 전
1. 입력 크기와 제한사항을 먼저 읽는다.
2. 예상 시간복잡도를 적는다.
3. 필요한 자료구조/알고리즘 후보를 생각한다.
4. 예제 한 개를 손으로 따라가며 규칙을 확인한다.
5. 기존 GitHub 풀이와 다른 사람의 정답은 보지 않는다.

## 풀이 시간 기준
- 기초/Level 0: 10분 내외
- Level 1: 15~20분
- Level 2: 25~30분
- 제한 시간이 지나도 진전이 없다면 현재 접근을 먼저 기록한다.

## 막혔을 때
정답을 바로 보지 않고 다음 순서로 도움을 받는다.

1. 문제와 제한사항을 다시 읽는다.
2. 작은 입력을 직접 만들어 손으로 실행한다.
3. 자료구조나 알고리즘 개념만 다시 확인한다.
4. 그래도 막히면 정답이 아닌 힌트를 확인한다.
5. 마지막에 해설/다른 풀이를 확인한다.
6. 참고해서 해결한 문제는 완료로 간주하지 않고 다음 날 다시 푼다.

Codex/AI를 사용할 때도 먼저 정답 생성을 요청하지 않는다. 풀이 후 시간복잡도, 엣지케이스, 비효율, 대안 풀이를 검토하는 코드 리뷰어로 활용한다.

## PyCharm 풀이 파일
기존 규칙을 따른다.

```text
solutions/programmers/<난이도>/<문제번호>_<문제명>.py
```

Programmers에서는 제출 함수가 핵심이므로 불필요한 입출력 코드를 추가하지 않는다.

```python
def solution(...):
    # 필요한 경우 핵심 판단만 짧게 주석으로 남긴다.
    answer = ...
    return answer
```

## 주석 원칙
코드를 그대로 한국어로 번역하는 주석은 작성하지 않는다.

좋은 주석:
- 왜 이 자료구조를 선택했는지
- 경계조건이 왜 필요한지
- 직관적이지 않은 로직의 의도
- 시간복잡도상 중요한 판단

불필요한 주석:
```python
# 배열을 반복한다
for item in items:
    ...
```

## 문제 풀이 기록
모든 문제에 긴 회고를 작성하지 않는다. 실패, 도움 사용, 새로운 패턴을 배운 문제를 중심으로 기록한다.

```md
### 문제명
- 결과: 성공 / 도움 후 성공 / 실패
- 소요 시간:
- 예상 시간복잡도:
- 실제 시간복잡도:
- 핵심 접근:
- 막힌 이유:
- 배운 점:
- 재풀이:
```

오답 원인은 다음 중 하나 이상으로 분류한다.

- 문제 이해
- 알고리즘 선택
- 시간복잡도
- 구현
- Python 문법
- 엣지케이스

## 풀이 후
1. Programmers 테스트를 통과시킨다.
2. 시간/공간복잡도를 다시 계산한다.
3. 더 단순한 풀이가 있는지 확인한다.
4. 필요한 경우 다른 사람 풀이 또는 Codex 리뷰와 비교한다.
5. 새로 배운 내용만 학습 기록에 남긴다.
6. 도움이 필요했던 문제는 다음 날 코드 없이 재풀이한다.

## Commit
문제 단위 커밋을 기본으로 한다.

```text
solve: programmers <문제번호> <문제명>
review: programmers <문제번호> <문제명>
docs: update coding test study notes
```

## Tistory 기록
문제별 정답 모음보다 재사용할 수 있는 지식을 Markdown으로 정리한다.

예:
- Python 코딩테스트 기본 문법과 라이브러리
- 문자열/배열 구현에서 자주 쓰는 패턴
- dict, set, Counter를 선택하는 기준
- 스택과 큐 문제 구분하기
- BFS/DFS 선택 기준
- 시간복잡도로 풀이 후보 줄이기

정답 코드 전체보다 선택 기준, 실수 포인트, 시간복잡도, 짧은 예제를 중심으로 작성한다.
